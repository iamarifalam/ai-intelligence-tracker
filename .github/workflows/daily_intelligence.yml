name: Ingest Daily AI Research

on:
  schedule:
    # 5 runs per day spread naturally across active hours:
    # 03:00 UTC (08:30 AM IST)
    # 07:00 UTC (12:30 PM IST)
    # 11:00 UTC (04:30 PM IST)
    # 15:00 UTC (08:30 PM IST)
    # 19:00 UTC (12:30 AM IST)
    - cron: '0 3,7,11,15,19 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run test suite
        run: |
          python -m unittest test_tracker.py

      - name: Run pipeline
        run: |
          python tracker.py

      - name: Commit dataset updates
        run: |
          git config --local user.name "Arif Alam"
          git config --local user.email "iamarifalam@users.noreply.github.com"
          git add data/ reports/
          git diff-index --quiet HEAD || (git commit -m "feat(data): update daily arXiv and Hugging Face index" && git push)
